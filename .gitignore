# macOS
.DS_Store

# Local Claude Code config (launch.json etc.)
.claude/

# Personal process data — never publish
skill-observations/
*handoff*.md

# Copyrighted NYS protocol PDFs — never publish
*.pdf
